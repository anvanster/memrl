# Rust build artifacts
.codegraph/
npm/
/releases/
.smelt/
.stellarion/
/target/
**/*.rs.bk
Cargo.lock

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local

# Tempera data (user-specific)
# Note: Don't commit personal episode data

# Model cache (downloaded on first run)
.fastembed_cache/

# Prebuilt binaries for npm distribution (built locally)
/prebuilt/

# Eval baselines are local runtime artifacts; keep the directory via .gitkeep
/evals/baselines/*.json
# Built binaries + DLLs in mcp-package (platform-specific, not the .js launcher)
mcp-package/bin/tempera-darwin-*
mcp-package/bin/tempera-linux-*
mcp-package/bin/tempera-win32-*
mcp-package/bin/tempera-mcp-darwin-*
mcp-package/bin/tempera-mcp-linux-*
mcp-package/bin/tempera-mcp-win32-*
mcp-package/bin/onnxruntime.dll
mcp-package/*.tgz
dist/
